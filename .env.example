# Document Service Configuration
# Copy to .env and adjust as needed.
#
# All variables below are OPTIONAL unless marked [REQUIRED].
# Variables shown commented out use their default values.

# ---------------------------------------------------------------------------
# Authentication
# ---------------------------------------------------------------------------

# Bearer token for API auth. Unset = dev mode (no auth required).
# [OPTIONAL] No default — when unset, all endpoints are open.
# SERVICE_API_KEY=your-secret-key-here

# ---------------------------------------------------------------------------
# Service Settings
# ---------------------------------------------------------------------------

# Enable debug logging
# [OPTIONAL] Default: false
# DEBUG=false

# Max upload size in bytes
# [OPTIONAL] Default: 52428800 (50MB)
# MAX_FILE_SIZE=52428800

# Temporary upload directory
# [OPTIONAL] Default: /tmp/document_uploads
# UPLOAD_DIR=/tmp/document_uploads

# CORS allowed origins (JSON list format)
# [OPTIONAL] Default: ["http://localhost:3000"]
# CORS_ORIGINS=["http://localhost:3000"]

# ---------------------------------------------------------------------------
# Docling Integration
# ---------------------------------------------------------------------------

# Docling service URL. Unset = Docling disabled (only fast_pdf + markitdown).
# [OPTIONAL] No default — when unset, Docling engine is unavailable.
# DOCLING_SERVICE_URL=http://docling:8080

# Docling request timeout in seconds
# [OPTIONAL] Default: 300
# DOCLING_TIMEOUT=300

# Verify SSL for Docling calls
# [OPTIONAL] Default: true
# DOCLING_VERIFY_SSL=true

# ---------------------------------------------------------------------------
# Triage Thresholds
# ---------------------------------------------------------------------------

# PDF block count threshold for routing to Docling (complex PDFs)
# [OPTIONAL] Default: 50
# PDF_BLOCK_THRESHOLD=50

# PDF image count threshold for routing to Docling
# [OPTIONAL] Default: 3
# PDF_IMAGE_THRESHOLD=3

# Number of PDF pages to analyze during triage
# [OPTIONAL] Default: 3
# PDF_PAGES_TO_ANALYZE=3

# Office file size threshold for Docling routing (bytes)
# [OPTIONAL] Default: 5242880 (5MB)
# OFFICE_SIZE_THRESHOLD=5242880
